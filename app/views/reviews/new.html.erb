<div class='container'>
  <h1>Add a review</h1>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.ipout :rating %>
    <%= f.input class: btn %>
  <%= end %>
</div>
